## WhisperX API

### 1. Introduction

Currently we are using modal server to deploy our whisperx api,

1. Docker image we are using is `whisperx-api:0.0.1`

### Installation

1. Git Clone
2. Pip install requirements.txt
3. run `modal serve modal_deployments.py`
4. Go the the url `modal_url/docs` for swagger
